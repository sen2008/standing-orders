// Entry point. UI modules land here as each MVP step (DESIGN.md §14) is built:
// map renderer, kingdom panel, bounty placement, hero panel, orders/confirm, event feed, scoreboard.

console.log('Standing Orders — frontend scaffold loaded');
